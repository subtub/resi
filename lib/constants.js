/**
 * Some constant variables we use over and over.
 */
module.exports = CONSTANTS = {
	/**
	 * The different token types.
	 */
  TOKEN_TYPE: {
    TEXT: 'text',
    FILE: 'file',
    WEBURL: 'weburl',
    SCRIPT: 'script'
  }
};
